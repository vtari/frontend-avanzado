import * as fromApp from './app.actions';
/* 
describe('App Actions', () => {
  describe('App Actions', () => {
    let params;
    beforeEach(() => {
      params = {
        payload: [
          {
            ou: 'andujar34',
            description: 'test',
          },
          {
            ou: 'andujar34',
            description: 'test',
          },
        ],
        message: 'App error' as string,
      };
    });
    describe('LoadGroup', () => {
      it('should create an action', () => {
        const action = new fromApp.LoadGroups();
        expect({ ...action }).toEqual({
          type: fromApp.LOAD_GROUPS,
        });
      });
    });
    describe('LoadGroupFail', () => {
      it('should create an action', () => {
        const action = new fromApp.LoadGroupsFailed(params.message);
        expect({ ...action }).toEqual({
          type: fromApp.LOAD_GROUPS_FAILED,
          payload: params.message,
        });
      });
    });
    describe('LoadGroupSuccess', () => {
      it('should create an action', () => {
        const action = new fromApp.LoadGroupsSuccess(params.payload);
        expect({ ...action }).toEqual({
          type: fromApp.LOAD_GROUPS_SUCCESS,
          payload: params.payload,
        });
      });
    });
  });
}); */
